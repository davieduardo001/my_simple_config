# Gerado pelo nwg-displays (Super+P) — este arquivo é sobrescrito por inteiro
# a cada "Apply", então não edite à mão esperando que sobreviva.
#
# Precisa existir mesmo vazio: o `source` no hyprland.conf falha se o arquivo
# não estiver lá numa instalação limpa.
#
# Sintaxe: monitor = NOME, RESOLUÇÃO, POSIÇÃO, ESCALA[, mirror, ORIGEM]
#   monitor = HDMI-A-1, preferred, auto-right, 1          # estende à direita
#   monitor = HDMI-A-1, preferred, auto, 1, mirror, eDP-1 # espelha o eDP-1
#
# Sem nenhuma linha aqui, vale o fallback `monitor = , preferred, auto, 1`
# definido no hyprland.conf.
