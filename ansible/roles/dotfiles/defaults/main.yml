---
dotfiles_links:
  - fastfetch
  - alacritty
  - rofi
  - hypr
  - waybar
  - swaync

# Configs que o app procura no $HOME, não em ~/.config
dotfiles_home_links:
  - { src: tmux/tmux.conf, dest: .tmux.conf }

dotfiles_file_links:
  - gtk-3.0/settings.ini
  - gtk-4.0/settings.ini
  - qt5ct/qt5ct.conf
  - qt6ct/qt6ct.conf
