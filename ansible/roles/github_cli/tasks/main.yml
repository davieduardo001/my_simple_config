---
- name: Check GitHub CLI auth status
  ansible.builtin.command: gh auth status
  register: gh_status
  changed_when: false
  failed_when: false

- name: Notify about manual GitHub CLI auth
  ansible.builtin.debug:
    msg: "GitHub CLI is not authenticated. Run 'gh auth login' manually after setup."
  when: gh_status.rc != 0
